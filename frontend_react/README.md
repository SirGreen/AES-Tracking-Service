
  # Children's Location Monitoring UI

  This is a code bundle for Children's Location Monitoring UI. The original project is available at https://www.figma.com/design/fqoPycxDigeNzRuRmtphCM/Children-s-Location-Monitoring-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  